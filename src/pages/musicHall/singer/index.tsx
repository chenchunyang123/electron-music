import React from 'react';
import styles from './index.less';

import MNav from '@/components/m-nav';

export default () => {
  return (
    <div className={styles.sg_wrap}>
      <div className={styles.sg_nav}>
        {/* <MNav activeKey={0} list={} callBack={} /> */}
      </div>
    </div>
  );
};
